name: Lock image codec dependencies

on:
  push:
    branches: [feat/commercial-v1]
    paths:
      - '.github/workflows/lock-image-codecs.yml'

permissions:
  contents: write

jobs:
  lock:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          ref: feat/commercial-v1
      - uses: pnpm/action-setup@v6
        with:
          version: 10.11.1
          run_install: false
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: pnpm
      - run: pnpm install --no-frozen-lockfile
      - run: pnpm format
      - name: Verify codec metadata
        run: |
          for package in @jsquash/jpeg @jsquash/png @jsquash/webp @jsquash/avif @jsquash/resize; do
            npm view "$package" version license repository.url
          done
      - name: Commit dependency lock
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add package.json pnpm-lock.yaml THIRD_PARTY_NOTICES.md
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m 'chore: lock image codec dependencies'
          git push origin HEAD:feat/commercial-v1
